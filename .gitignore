# Secrets — never commit
config/secrets.local.ps1

# Rendered outputs (generated locally by install.ps1)
.cloud-init-rendered/
config/rendered/

# OS / editor
.DS_Store
Thumbs.db
*.swp
install-run.log

# Graphify (generated knowledge graph — commit graphify-out/ except local-only files)
graphify-out/cost.json
graphify-out/cache/
